name: amway-bee-safe-us

services:
  frontend:
    build:
      context: ../..
      dockerfile: Docker/production-us/Dockerfile
    image: bee_safe_frontend
    container_name: bee_safe_frontend
    networks:
      - amway-bee-safe-us
    restart: always

networks:
  amway-bee-safe-us:
    driver: bridge
