name: amway-bee-safe-my

services:
  frontend:
    build:
      context: ../..
      dockerfile: Docker/production-my/Dockerfile
    image: bee_safe_frontend
    container_name: bee_safe_frontend
    ports:
      - "8080:80"
    networks:
      - amway-bee-safe-my_default
    restart: always

networks:
  amway-bee-safe-my_default:
    external: true
