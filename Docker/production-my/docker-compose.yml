name: amway-bee-safe-my

services:
  frontend:
    build:
      context: ../..
      dockerfile: Docker/production-my/Dockerfile
    networks:
      - amway-bee-safe-my_default
    restart: always

networks:
  amway-bee-safe-my_default:
    external: true
