name: amway-bee-safe-us

services:
  frontend:
    build:
      context: ../..
      dockerfile: Docker/Dockerfile
    networks:
      - amway-bee-safe-us_default
    restart: always

networks:
  amway-bee-safe-us_default:
    external: true
